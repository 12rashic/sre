apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sre.fullname" . }}-test-connection"
  labels:
    {{- include "sre.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sre.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
