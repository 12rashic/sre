apiVersion: v1
kind: Service
metadata:
  name: {{ include "sre.fullname" . }}
  labels:
    {{- include "sre.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sre.selectorLabels" . | nindent 4 }}
